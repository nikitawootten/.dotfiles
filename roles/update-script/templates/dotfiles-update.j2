#!/usr/bin/env bash
# Note: this file is generated from the update-script role (found in {{ playbook_dir }}/roles/update-script).

echo "Running Play {{ playbookBaseName }}"

(cd {{ playbook_dir }}; ansible-playbook -i hosts -K {{ playbookBaseName }} $@)
